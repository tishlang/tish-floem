{
  "name": "hello-floem-example",
  "private": true,
  "version": "0.0.0",
  "description": "Sample native app: tishlang_ui JSX + tish:floem run()",
  "scripts": {
    "build": "cargo run --manifest-path ../../../tish/Cargo.toml -q -p tishlang -- compile --target native --native-backend rust src/main.tish -o dist/hello-floem",
    "dev": "npm run build && ./dist/hello-floem",
    "run": "npm run dev",
    "start": "npm run dev"
  }
}
