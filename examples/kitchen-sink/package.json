{
  "name": "kitchen-sink-example",
  "private": true,
  "version": "0.0.0",
  "description": "Floem UI intrinsics demo (Tish JSX → tishlang_floem host)",
  "scripts": {
    "build": "cargo run --manifest-path ../../../tish/Cargo.toml -q -p tishlang -- compile --target native --native-backend rust src/main.tish -o dist/kitchen-sink",
    "dev": "npm run build && ./dist/kitchen-sink",
    "run": "npm run dev",
    "start": "npm run dev"
  }
}
